<%= render partial: 'public/shared/header' %>

<div class="container mt-5">
  <div class="jumbotron">
    <h1 class="display-4">ようこそ、ながのCAKEへ！</h1>
    <p class="lead">このサイトは、ケーキ販売のECサイトになります。会員ではない方も商品の閲覧はできますが、購入には会員登録が必要になります。</p>
  </div>

  <h2 class="mt-4">新着商品</h2>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-md-3">
        <img src="<%= item.image %>" alt="商品画像">
        <h5 class="mt-3"><%= item.name %></h5>
        <p class="mb-3">¥<%= number_to_currency(item.price) %></p>
      </div>
    <% end %>
  </div>
  <div class="col-md-12 mt-4">
    <%= link_to "全ての商品を見る", public_customer_items_path, class: "btn btn-primary" %>
  </div>
</div>
