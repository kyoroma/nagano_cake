<style>
  .jumbotron {
    /*background-image: url('<%= asset_path('default-image.jpg') %>'); /* アセットパイプラインのパスを指定 */
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
  }
</style>

<%= render partial: 'public/shared/header' %>

<div class="container mt-5">
  <div class="jumbotron">
    <div class="row", style="width: 600px; margin-left: 100px;">
      <div class="col">
        <p>ようこそ、ながのCAKEへ！<br>このサイトは、ケーキ販売のECサイトになります。<br>会員ではない方も商品の閲覧はできますが、<br>購入には会員登録が必要になります。</p>
      </div>
    </div>
  </div>

  <h2 class="mt-2">新着商品</h2>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-md-3">
        <% if item.image.attached? %>
          <%= link_to item_path(item) do %>
            <%= image_tag item.image, class: "img-fluid" %>
          <% end %>
        <% else %>
          <p>画像がありません</p>
        <% end %>
        <p class="mt-3"><%= item.name %></p>
        <p class="mb-3">¥<%= item.price.to_i %></p>
      </div>
    <% end %>
  </div>
  <div class="col-md-12 mb-3 text-right">
    <%= link_to "全ての商品を見る>", items_path %>
  </div>
</div>
